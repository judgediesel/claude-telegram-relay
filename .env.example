# Required
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_USER_ID=your_telegram_user_id

# Optional - Paths
# CLAUDE_PATH=claude                    # Path to claude CLI if not in PATH
# RELAY_DIR=~/.claude-relay             # Working directory

# Optional - Voice (if you want voice message support)
# GEMINI_API_KEY=                       # For voice transcription
# ELEVENLABS_API_KEY=                   # For voice responses
# ELEVENLABS_VOICE_ID=                  # Your preferred voice

# Optional - Webhook HTTP server
# WEBHOOK_SECRET=                       # Enables HTTP API (generate with: openssl rand -hex 32)
# WEBHOOK_PORT=3100                     # Default port

# Optional - Memory persistence (if using Supabase)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# Optional - Scheduled check-ins (requires Supabase)
# CHECKIN_INTERVAL_MINUTES=30            # How often to evaluate (default: 30)

# Optional - Google Calendar integration (service account, read/write)
# GOOGLE_CALENDAR_KEY_FILE=~/.claude-relay/google-service-account.json
# GOOGLE_CALENDAR_ID=primary             # Calendar ID or "primary"

# Optional - Gmail integration (requires service account with domain-wide delegation)
# GMAIL_USER=your@email.com              # Email to read via service account

# Optional - Twilio SMS/Voice (for SMS and phone call escalation)
# TWILIO_ACCOUNT_SID=                    # From twilio.com/console
# TWILIO_AUTH_TOKEN=                     # From twilio.com/console
# TWILIO_PHONE_NUMBER=+1XXXXXXXXXX      # Your Twilio number
# TWILIO_USER_PHONE=+1XXXXXXXXXX        # Your personal cell phone
# TWILIO_PUBLIC_URL=                     # ngrok URL for inbound calls/voice conversations

# Optional - Toll-free SMS number (bypasses 10DLC, requires verification)
# TWILIO_SMS_NUMBER=+1XXXXXXXXXX          # Separate toll-free number for SMS delivery

# Optional - Daily briefing & end-of-day recap
# DAILY_BRIEFING_HOUR=8                  # Hour (24h) to send morning briefing (default: 8)
# END_OF_DAY_HOUR=18                     # Hour (24h) to send end-of-day recap (default: 18)
